export type OptionRecord = {
  id: string;
  name: string;
  added_at?: string;
};

export type SchoolCourseRecord = {
  id: string;
  school_name: string;
  course_name: string;
  added_at?: string;
};
